name: export-to-gmail-pull-request

on:
  pull_request:
    paths:
      - 'modules/export-to-gmail/**'
      - '.github/workflows/export-to-gmail-pull-request.yml'

env:
  JAVA_VERSION: 21
  WORKING_DIRECTORY: modules/export-to-gmail
  MODULE_NAME: export-to-gmail

permissions:
  contents: read
  checks: write

jobs:
  tests:
    if: ${{ github.event.pull_request.draft == false }}
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Java ${{ env.JAVA_VERSION }}
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: ${{ env.JAVA_VERSION }}

      - name: Compile only
        working-directory: ${{ env.WORKING_DIRECTORY }}
        run: mvn -B clean compile
        
      # - name: Run tests
      #   working-directory: ${{ env.WORKING_DIRECTORY }}
      #   run: mvn -B test

      # - name: Publish JUnit report
      #   if: always()
      #   uses: mikepenz/action-junit-report@v4
      #   with:
      #     report_paths: ${{ env.WORKING_DIRECTORY }}/target/surefire-reports/*.xml
      #     include_passed: true
      #     annotate_only: true
      #     job_summary: true

      # - name: Upload Surefire reports artifact
      #   if: always()
      #   uses: actions/upload-artifact@v4
      #   with:
      #     name: ${{ env.MODULE_NAME }}-test-reports
      #     path: ${{ env.WORKING_DIRECTORY }}/target/surefire-reports/
      #     if-no-files-found: warn
